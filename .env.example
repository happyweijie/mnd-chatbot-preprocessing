# Copy to .env for local runs or on SageMaker Studio
# (real environment variables always take precedence over .env values).

# required for phase 4 (embedding)
OPENAI_API_KEY=sk-...

# optional: gov endpoint; scheme is added automatically if missing
# OPENAI_BASE_URL=https://api.ai.tech.gov.sg/platform/models

# optional: embedding model (default: text-embedding-3-small)
# EMBEDDING_MODEL=text-embedding-3-large

# optional: embedding dimensions; leave unset to use the model's default
# (1536 for text-embedding-3-small, 3072 for text-embedding-3-large)
# EMBEDDING_DIM=1536

# S3 batch runs (`python -m pipeline batch`); layout in README "S3 storage layout".
# bare bucket name and a bare key prefix (no s3://, no leading/trailing slash)
# HINT_BUCKET=my-gov-bucket
# HINT_S3_PREFIX=teams/xyz/hint
# local scratch dir; keep it on persistent storage (EFS on Studio) so an
# interrupted embedding run can resume from its checkpoint
# HINT_WORK_DIR=s3_work
